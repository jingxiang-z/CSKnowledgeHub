/*
 * rwlock_example.c
 * 
 * Demonstrates reader-writer locks.
 * Shows how multiple readers can access data concurrently,
 * but writers need exclusive access.
 * 
 * Compilation: gcc -o rwlock_example rwlock_example.c -pthread
 * Usage: ./rwlock_example
 */

#include <stdio.h>
#include <stdlib.h>
#include <pthread.h>

int main() {
    // TODO: Implement reader-writer lock examples
    printf("Reader-writer lock example - To be implemented\n");
    
    return 0;
}

